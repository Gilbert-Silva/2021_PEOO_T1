using System; 
class Program { 
  public static void Main() {
    int k; 
    // Declarar uma variável
    // 1. Reservar um local na memória para guardar um dado
    // 2. Dar um nome para esse local na memória
    // 3. Definir que valores a variável pode guardar
    // 4. Definir que operações a variável pode fazer
    k = 5;
    k = k + 8;
    //k.Substring(4);

    // 8 GBytes de Ram: 0 até 8.589.934.591

    string s = "Teste";
    Console.WriteLine(s.Substring(1));
  }
}

Tipo int    - O que eu guardo (atributo), o que eu faço (método, operador)
Tipo string - O que eu guardo (atributo), o que eu faço (método, operador)

Tipo enum

Tipo struct
Tipo class
Tipo interface

Tipo dynamic
Tipo object // classe específica


